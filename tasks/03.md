# Template de Prompt para Criar uma Rota de API

Crie a rota `GET /workout-plans/:id`

## Descrição

Essa rota deve retornar o plano de treino recebido e seus dias. Para os dias do plano de treino

## Requisitos Técnicos

- NÃO RETORNE OS `workoutExercises`, apenas `exercisesCount`

## Autenticação

- Rota protegida.
- Apenas o dono (usuário) pode ver o seu plano de treino.

## Request

```ts
interface Body {}
```

```ts
interface Params {
  date: string;
}
```

```ts
interface Query {}
```

## Response

```ts
interface StatusCode200 {
  id: string;
  name: string;
  workoutDays: Array<{
    id: string;
    weekDay: string;
    name: string;
    isRest: boolean;
    estimatedDurationInSeconds: number;
    exercisesCount: number;
  }>;
}
```

## Regras de Negócio

N/A
